# RP18r 指示書: RP18 の指摘の修正（P18e）の確認のレビュー

最初に、作業記録置き場（`.superpowers/sdd/2026-08-27-beaksight-implementation-plan/`）の `RP18-review-brief.md` を読んでください。厳守事項、重大度の基準、報告の形式は、その指示書に従ってください。

`R-review-common.md` の末尾の「一時ディレクトリの扱い」も、必ず守ってください。一時ディレクトリに、リポジトリへのリンク（ジャンクション、シンボリックリンク）を作ってはいけません。

## 担当

Guard の独立レビュー RP18（`RP18-review-result.md`）の指摘が、P18e（`P18e-report.md`）で解消したかを確かめてください。

- 対象の指摘: Important の1、Minor の2・3
  - 指摘4は Task 18 で扱い、指摘5は設計者が受け入れました。対象外です。
- 指摘ごとに、次のどれかと、その根拠を書いてください。
  - 解消
  - 一部解消
  - 未解消

あわせて、次のことも確かめてください。

- P18e のそのほかの変更に、新しい問題がないこと
  - 期限の値の検証の移動
  - テスト補助の期限
  - CC-028 の残り
  - CC-025
  - BOM の表記
- Task 18 の前の整理の範囲に、Important 以上の問題が残っていないこと

設計書: `doc/design/2026-09-25-beaksight-pre-task-18-cleanup-design.md`（とくに 4.2 の、RP18 を受けて明確にした部分）

## とくに確かめること

1. **指摘1（完了の正直さ）**
   - 環境の読み取りの Context の終了だけが期限を過ぎた場合に、Run が `COMPLETE` にならず、理由が付くこと。
   - ほかの呼び出し元でも、閉じる処理の期限切れを捨てている所がないこと。
     - 対象: PREFLIGHT、サイトの metadata、Page Auditor、幅の走査、Interaction
   - 可能なら、RP18 と同じく、終わらない偽の Browser で、実際に確かめてください。
2. **指摘2（Run 全体の期限の注入とテスト）**
   - Run Coordinator から下へ、期限が渡ること。
   - 追加されたテストが、実際の時間を待たずに、Run が確定して返ることを確かめていること。
   - そのテストが、指摘1の再発を検出できること。
3. **指摘3（幅の走査）**
   - Context を閉じずに投げる経路が、なくなっていること。
4. **安全の境界**
   - Guard と factory が変わっていないこと。
   - `safety-ledger.ts` の変更が、型だけであること。

## 報告

`RP18-review-brief.md` の形式で、日本語で返してください。

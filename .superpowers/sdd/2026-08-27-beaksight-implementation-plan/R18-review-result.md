# R18 レビュー結果（要約。設計者が保存。Task 18 のチェックポイント）

## 総合判定

承認。Critical 0件、Important 0件、Minor 4件。

## Gate の一覧

| 種類 | 判定 |
| --- | --- |
| Safety S01〜S10 | そろっている。35件 PASS。サーバの境界で数え、対照で「数えられる」ことを示している |
| Auditor A01〜A10 | そろっている。16件 PASS。実際の Chromium と実際の Rule |
| Architecture ARCH01〜08 | そろっている。PASS。検出の関数に単体テストがある。除外は、使われなくなると FAIL する |
| UI01〜06 | そろっている。PASS |

- `it.skip`、`it.todo`、`it.only`、`skipIf`、`runIf` は、Gate のファイルと補助のどこにもない。
- 実装タスク指示 第11章の条件（1件でも FAIL していれば、実サイトの smoke をしない）を、今の状態は満たしている。

## レビュー担当の独自の確認

- T18e の修正は、`classifyUrl` で判定し、前の判定と意味が同じである。
- DEF-011 で漏れていたのは、`normalize-url.ts:161` の1行だけだった。今は検査されている。
- 一時ビルドの CLI で、`/crawl/index.html` から最後まで監査した。
  - 17秒で終了コード 0、COMPLETE だった。ページは発見5・監査5、指摘は ERROR 7、WARN 10、INFO 4 件だった。
  - 違反は0件で、サーバに届いたのは GET の27件だけだった。
  - すべての artifact が、スキーマに合った。
  - バンドルの中の run.json は、書き出したものとバイト単位で同じだった。
- report.html
  - 指摘の21行すべてに、`page.json` と JSON ポインタへのリンクがある。ポインタの28件は、すべて正しい。
  - スクリーンショットへのリンクは70件で、ファイルはすべてある。
  - href は `#`、`http:`、`pages/` だけで、`<script>` はない。

## Minor と、設計者の判断

1. M1: ARCH04 の検出が、`allowedOrigins.has(new URL(url).origin)` などの書き方を見逃す。→ Task 19 の前の整理（C18）で、検出を広げる。
2. M2: S08 の Interaction の Gate が、click が行われたこと（status が `REJECTED_UNSAFE` でないこと）を確かめていない。→ C18 で直す。
3. M3: S03 は、候補の方針による拒否の経路だけを確かめている。ページのスクリプトが `location.href='tel:…'` のように移動する経路は、確かめていない。
   - Guard は `context.route('**/*')` を使うので、http(s) 以外のスキームへの移動は横取りされない（推測）。
   - headed の実行で、外部のアプリを起動しうるかは、確かめていない。
   - → **安全に関わるので、C18 の最初に調べる。**
     - 実装計画の Task 20 は、headed で実サイトの smoke を行う。そのため、Task 19 の前に確かめる。
     - 起動しうる経路があれば、既存不具合として登録し、直す。
4. M4: UI Gate の文字列の取り出しで、入れ子のテンプレートを取り違える。→ CC-030 として登録済み。C18 で行う。
5. 参考: `src/audit/safety-rules.ts` の変数名 `example`（27か所）は、実装タスク指示 第6章の文言（`example` を `src/**` に書かない）に反する。
   - → **C18 で、名前を変える**（例: `sample`）。費用が小さく、文言にも合わせられるためである。
   - 設計書 4.4 の「一般の語は違反にしない」という ARCH01 の規則は、そのまま残す。

## 確認できなかった点

- headed の実行での、外部スキームへの移動の振る舞い（M3）。C18 で調べる。
- 実サイトの設定は、`config/targets/` にまだない。ARCH01 が実サイトの識別子を照らせるのは、その設定を置いた後である。`src/` に実サイトの名前の文字列がないことは、grep で確かめた。

# Task 1〜5 レビュー（2026-09-23、独立レビュー担当）

総合判定: 修正が必要（Critical 0 / Important 4 / Minor 5）

SSOT（`loadConfig` はCLIだけが呼ぶ、`a[href]` の抽出は `discover-links.ts` だけ、request-policy への委譲）、Target Isolation（`src/**` に `example`・対象domain・絶対URLが0件）、GET/HEAD以外のブロックは成立している。

## 指摘

- **R1 / Task 1 / Important**（`src/config/load-config.ts:117`、`src/config/types.ts:6-40`）: `loadConfig()` が `target` を取り除くため、`AuditConfig` に `target.id` がない。`createFindingFingerprint()` は `targetId` を必須としている（`src/core/ids.ts:24`）。Task 20/21 の報告にも必要。期待: 確定後の不変な設定に `target.id` を含める。
- **R2 / Task 1 / Important**（`load-config.ts:64-73, 96-114`）: `--config` が `config/targets` の外にあると、その置き場所以下の `*.json` を再帰的にすべて読む。`loadConfig('schemas/fixture-audit.json')` は `audit.schema.json` で失敗した。Task 19 の `./tmp` の手順では、出力された JSON によって2回目以降が設定エラーになる。期待: `target.id` の一意性の確認は `config/targets` だけを対象にする。
- **R3 / Task 5 / Important**（`src/safety/safety-ledger.ts:202-252`）: Safety Ledger の記録上限への到達（`SAFETY_LEDGER_LIMIT_REACHED`）が `invariantViolations` に入る。POST のブロックを257回記録しただけで、Run が `ABORTED_BY_SAFETY` になる。記録が満杯のときは、本物の違反の最後の1件が上書きされる。設計 9.5 に反する。期待: 記録の打ち切りは「記録が不完全」という事実として扱い `PARTIAL` にする。本物の違反とは分ける。
- **R4 / Task 2 / Important**（`src/core/contracts.ts:16-61, 105-126`、`schemas/page.schema.json`、`schemas/run.schema.json`）: 共通の型とschemaが、後続Taskで必要な表現を持っていない。
  - Evidence の payload が最小限の形で、collector はどれもこの型を使っていない。
  - 性能の状態に `UNSUPPORTED` がない。
  - Link と Color の Evidence の種類がない。
  - Desktop/Mobile ごとの状態がない（設計 9.3）。
  - `run.json` に設計 18.1・第35章の項目（Safety Ledger、`startUrl`、environment、ブロック件数など）がない。
  - `additionalProperties:false` なので、項目を足すと schema が壊れる。
  期待: Task 14 より前に、schema の版を上げ、owner を1つのまま型と schema を拡張する計画を決める。
- **R5 / Task 1 / Minor**（`validate-config.ts:58-66, 83-87`）: `https://u:p@host` のような認証情報つきの `allowedOrigins` と、小数の `maxPages` が検証を通る。前者は実行時に捨てられ、巡回できるOriginが0件になる。CC-004 の2つの正規化は同じ結果を返すことを確認済み。
- **R6 / Task 2 / Minor**（`src/core/status.ts:125-131`）: `safetyInvariantViolations` が `Infinity` や `1.5` のとき `PARTIAL` を返す。
- **R7 / Task 3 / Minor**（`src/crawl/discover-links.ts:62-75, 104`）: `policy` を省略すると、暗黙の受け入れ判定が使われる。期待: `policy` を必須にする。正規化したURLに認証情報が残る（`normalize-url.ts:66`）。
- **R8 / Task 4/5 / Minor**（`fixtures/server.ts:78-91`）: OPTIONS や独自メソッドがサーバのカウンタの対象外で、サーバに届かないことの証明がない。
- **R9 / テスト / Minor**（`tests/unit/cli.test.ts:13-20`、`tests/unit/schema-validator.test.ts:104-114`）: 単体テストが内部で `tsc` を実行して `dist/` を書き換える。並列実行で競合するおそれがある。

## 再実行

Task 1〜5 の対象テスト13ファイル: 155件 PASS。`passive-request-guard.test.ts`: 117件 PASS。`npm run typecheck`: PASS。

# F16 実装報告（要約。設計者が保存）

## 結論

完了した。`npm run verify` は PASS した（41ファイル、1447件）。

- 下準備で hover の後に focus するようにした。
- 安定性の確認を加えた（`STABILITY_WINDOW_MS = 500`、100ms ごとに観測する）。
- 持続の確認を加えた（`PERSISTENCE_WINDOW_MS = 500`）。
- `<details>` の開閉を、`detailsOpen` として記録するようにした。
- hover の後の状態を、探し直しの目印にするようにした。
- 変わった属性の名前を、`changedAttributes` として残すようにした（上限は32件）。
- 理由の整形の残りを直した。
- 何もしないボタン7種類は NOT_VERIFIABLE になった。何かが起きる部品9種類は VERIFIED になった。
- `isolated-interaction` は、2回続けて実行し、2回とも292件が PASS した。
- 候補1つあたりの時間は、VERIFIED の場合で約1.0秒長くなった。

## 実装者の判断と、設計者の判断

1. 下準備の handle は、安定性の確認の後に破棄する。同じ要素の、hover の後の状態を取るためである。→ 承認する。設計書 4.4.2 の手順を更新する。
2. hover の前に、下準備の handle が探索で見つけた候補と同じかを確かめる。違う場合は SKIPPED にする。→ 承認する。設計書に書き加える。
3. 値が空白だけの `class` と `style` は、属性がないものとみなす。→ 承認する。設計書 4.4.1 に書き加える。
4. 比べられない観測が安定性の確認の途中にあった場合は、属性のすべてを不安定とみなす。→ 承認する（fail-closed）。
5. `interactionTimeoutMs` が約500ms以下だと、どの候補も NOT_VERIFIABLE になる。→ F17 で、設定の検証に下限を設ける。
6. 大きなページでは、DOM の作業量が尽きやすくなる。→ 許容する。結果は正直である。
7. `changedAttributes` を切り詰めたことが、Evidence に残らない。→ F17 で、切り捨ての印を加える（切り捨てを隠さない）。
8. 共通部品台帳の更新。→ 設計者が行う。
9. 400ms の既存の fixture は、安定性の確認の間に変わるようになった。→ 許容する。新しい fixture で確かめている。

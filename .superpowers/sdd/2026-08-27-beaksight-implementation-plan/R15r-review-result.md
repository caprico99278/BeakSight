# R15r R15e・C15x の確認のレビューの結果（2026-09-24。Task 15 のチェックポイントの判定）

総合判定: 承認（Critical 0 / Important 0 / Minor 4）

Task 15 のチェックポイントを通過した。

## 前回の指摘の解消

次の指摘は、すべて解消した。

- I1（テストの検出力）
- I2（Ledger の登録）
- Minor-1〜4、Minor-6

C15x で直した DEF-006、DEF-007、CC-021 に、新しい問題はない。

## Minor と、設計者の判断

1. Context を閉じる処理と、Context・page を作る処理に、期限がない（`passive-request-guard.ts:429`）。
   - 判断: DEF-008 として登録する。
   - Guard の範囲なので、Task 18 の前の整理で、Guard のレビューとあわせて扱う。
2. Browser を閉じる期限を過ぎた後に、Chromium のプロセスが残り、Node が終わらないおそれがある（推測）。
   - 判断: Task 17 の CLI（U17a）で、終わらせ方を決める。
   - 書き出しと表示が終わった後は、終了コードを決めて、プロセスを確実に終わらせる。
3. 例外でクロールが止まった経路では、実行時間の超過が記録されない。
   - 判断: Task 18 の前の整理で直す。Run は `PARTIAL` になるので、影響は小さい。
4. 期限のテストが、実際の時間を待っている（5〜10秒）。
   - 判断: Task 18 の前の整理で、期限を注入できるようにし、テストを短くする。

# Task 3 filesystem baseline

Git operations are prohibited.

- `src/crawl/**` did not exist before Task 3.
- `tests/unit/normalize-url.test.ts`, `tests/unit/admission-policy.test.ts`, `tests/unit/crawl-queue.test.ts`, and `tests/component/discover-links.test.ts` did not exist.
- Shared branded IDs and closed Evidence contracts are provided by Task 2 in `src/core/contracts.ts` and `src/core/ids.ts`; Task 3 must consume them without redefining core identity semantics.

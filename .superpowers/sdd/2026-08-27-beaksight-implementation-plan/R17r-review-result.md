# R17r レビュー結果（要約。設計者が保存）

## 総合判定

承認。Critical 0件、Important 0件、Minor 3件。Task 16 と Task 17 を完了とする。

## R17 の指摘の判定

- 指摘1（出力先のエラー）: 解消
  - 一時ビルドの CLI で、次の場合を確かめた。どれも、スタックトレースは出なかった。
    - `--help` で出力先を閉じた場合: 終了コードは 0 だった。
    - `run` で出力先を閉じた場合: 終了コードは Run Status のとおりだった。
    - 扱われない例外と reject: 日本語の文言と、終了コード 1 だった。
  - 終了の後に、Chromium は残っていなかった。
  - 修正の前の状態を差し込みで再現すると、テストが後退を検出した。
- 指摘2（導き直した Run Status）: 解消
- 指摘3（オプションの重複）: 解消
- 指摘4（BOM）: 解消

## Minor と、設計者の判断

1. 扱われない失敗での後始末の記述が、実装と合っていない（`src/cli/index.ts:8`、設計書 第7章）。
   - → 設計書は、設計者が直した。
   - → コードのコメントは、P18d で直す。
2. 本番の `run` が `finishAuditRun` を通ることを、固定するテストがない（`run-command.ts:153-154`）。→ P18d で、構造のテストを加える。
3. BOM を、見えない文字のまま書いている（`load-config.ts:62`、`config.test.ts:677`）。→ P18e で、`'\uFEFF'` と書く形に直す。

## 確認できなかった点

- ABORTED_BY_SAFETY の Run を、CLI で最後まで通すこと（Task 18 の Gate で確かめる）
- PowerShell の実際のコンソールで、パイプを閉じた場合

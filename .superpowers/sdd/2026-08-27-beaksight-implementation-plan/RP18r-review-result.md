# RP18r レビュー結果（要約。設計者が保存）

## 総合判定

承認。Critical 0件、Important 0件、Minor 2件。Task 18 の前の整理を完了とする。

## RP18 の指摘の判定

- 指摘1（Important、完了の正直さ）: 解消
  - 環境の読み取りの閉じる処理の失敗は、`onCloseFailure` を経て、Run の理由になる。
  - ほかの呼び出し元にも、期限切れを捨てて COMPLETE になる所はない。
  - 実際の Chromium で、PREFLIGHT の後に作る Context の `close()` を、すべて止めて確かめた。
    - Run は、6.5秒で `PARTIAL` になった。
    - 理由は3件付いた。
    - 違反は0件だった。
- 指摘2（Run 全体の期限の注入とテスト）: 解消
  - 追加したテストは、指摘1の再発を検出できる。
  - 期限を下へ渡し忘れた場合も、時間の上限で検出できる。
- 指摘3（幅の走査）: 解消

独立の確認（実際の Chromium）の結果:

| 場合 | 結果 |
| --- | --- |
| 閉じる処理が止まる | 6.5秒で `PARTIAL` |
| 作成が止まる | 2.3秒で `PARTIAL` |
| 何も止まらない | 3.6秒で `COMPLETE` |

どの場合も、Browser は閉じ、違反は0件だった。

## Minor と、設計者の判断

1. PREFLIGHT は、作成の期限切れと、失敗が決まっている場合に、閉じる処理の失敗をメッセージに残さない（`preflight.ts:305-313`）。
   - この場合の Run は `FAILED` なので、完了の正直さは保たれる。
   - → 共通部品台帳の「捨てない」の書き方に、この例外を明記した。
   - → PREFLIGHT のメッセージに閉じる処理の失敗を加えることは、Task 18 の後の整理で行う。
2. 幅の走査の閉じる処理の期限切れが、理由 `stress-layout:EVALUATION_FAILED` として記録され、期限切れだったことが読み取れない。
   - 以前からの分類で、後退ではない。
   - → Task 18 の後の整理で、期限切れが分かる理由にする。

## 確認できなかった点

- T18a が作業中のため、`isolated-interaction.test.ts` と fixture を広く使うテストは、実行していない。
- 実際の Chromium で、`context.close()` が自然に止まる状況の再現はしていない。

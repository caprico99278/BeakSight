# R16 レビュー結果（要約。設計者が保存）

## 総合判定

修正が必要。Critical は0件、Important は1件、Minor は5件だった。

問題がないと確かめられたもの:

- HTML の安全: エスケープ、`classifyUrl` だけで決まるリンク、安全なアンカー、CSP、UI03
- A10 と ARCH05、ARCH08
- 一時ファイルと rename、UTF-8 と LF、パスの検証、ID の形の検証
- 表示用モデルの件数の出どころ、再試行の前の記録の区別、「未観測」の表示
- バンドルの中身・順・決定論（時間帯を4つ変えて確かめた）、生の本文を含めないこと
- report は、playwright も orchestration も読み込まない。

レビュー担当が再実行したテスト:

- 対象の17ファイルで、676件が PASS した。
- UI Gate は、304ms で終わった。
- typecheck は PASS した。

## 指摘と、設計者の判断

1. **Important**: Safety の事象の表で、遮断した操作の URL がリンクになる（`html-report.ts:388`）。
   - 対象は、外部への作用、ダウンロード、POST などの http(s) の URL である。
   - 上位の設計書 第20章（914行）の「外部アクションURLはクリック可能リンクにせず、テキスト/コピー用途として表示する」に反する。
   - → **直す**（R16f）。Safety の事象の表の URL は、種類を問わず、すべて文字で示す（設計書 6.1.11）。
   - 原因: 設計者が 6.1.10 で事象の表を加えたときに、第20章の記述を確かめなかった。設計者の誤りである。
2. **Minor**: UI04 が、分割代入で書いた数え直しを検出できない（`ui-ssot.test.ts:113-130`）。→ 直す（R16f）。
3. **Minor**: HTML の部品に、相対パスの別の検証（`SAFE_RELATIVE_HREF_PATTERN`）がある。→ 直す（R16f）。
   - core の `isPortableRelativeArtifactPath` で確かめる。
   - そのうえで、href に固有の処理（区切りごとのパーセント符号化など）だけを、部品に置く。
4. **Minor**: 同じ UTC の秒に始めた2つの Run が、同じディレクトリに書く。→ **既存不具合 DEF-009** として登録する。
   - Run Coordinator の開始の処理に関わる。そのため、Task 18 の前の整理で直す。
5. **Minor**: 行がない表で、見出しが消え、何の表か分からない「なし」が並ぶ。→ 直す（R16f）。
6. **Minor**: 2つの問題がある。→ 直す（R16f。設計書 6.1.11）。
   - バンドルの `evidence-index.json` の参照先（`page.json`）が、ZIP に入っていない。
   - スクリーンショットの大きさに、上限がない。

## 確認できなかった点

- ブラウザでの見た目
- 実際の規模でのバンドルの大きさ
- CLI とのつなぎ込み（U17a）

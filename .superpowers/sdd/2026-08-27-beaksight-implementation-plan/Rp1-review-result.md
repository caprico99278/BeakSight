# R'1 Interaction の修正の確認のレビューの結果（2026-09-23）

総合判定: 修正が必要（Critical 0 / Important 1 / Minor 1）

## 前回の指摘の解消

- R1 の Minor（使われていない型）と Minor 3（`position: fixed`）: 解消した。
- R2 の N3・N4: 解消した。
- R2 の N1: 一部解消にとどまる。対象を何も覆っていない場合は、いくつかの形のページで正しく判定されることを確かめた。しかし、覆われている場合が残る（下の N1'）。
- 安全の性質: 判定の順序、Guard、fail-closed、下準備の間の通信に対する Guard の判定、handle の後片付けは、変わっていないことを確かめた。unhandledRejection は0件だった。

## 新しい指摘と、設計者の判断

- **N1' / Important**（`isolated-auditor.ts:694`、`:790`）
  - 指摘: 対象が固定表示の要素に覆われていると、Playwright は click を再試行しながらスクロールする。そのため、内側の要素がスクロールするページで、何もしないボタンが VERIFIED になる。ボタンが初めから画面内にあり、ただ覆われている場合にも起きる。
  - 判断: 設計書 4.4 の前提（click のときにスクロールしない）は、成り立たない場合があった。そこで、スクロールが起きうることを前提にした方式を加える。
    - 観測の前後で、対象のスクロールする祖先（`window` を含む）のスクロール位置を記録する。
    - いずれかの位置が変わっていた場合は、`boundingBox` の変化を、変化の根拠にしない。
    - ほかの変化の根拠（属性、テキスト、DOM の変化など）は、これまでどおり使う。
    - F10 で実装する。
- **Minor**（`isolated-auditor.ts:578-584`）
  - 指摘: `remaining() === 0` の検査が、2回続いている。
  - 判断: F10 で1つにまとめる。

## 再実行の記録

- `isolated-interaction.test.ts`: 2回続けて実行し、2回とも178件が PASS した。
- `passive-request-guard.test.ts`: 120件が PASS した。
- typecheck: PASS した。
- `127.0.0.1` 上で、約14のページを使って実機で確かめた。
